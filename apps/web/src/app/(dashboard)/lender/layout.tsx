import { LenderLayoutShell } from './_components/LenderLayoutShell'

export default function LenderLayout({ children }: { children: React.ReactNode }) {
  return <LenderLayoutShell>{children}</LenderLayoutShell>
}
